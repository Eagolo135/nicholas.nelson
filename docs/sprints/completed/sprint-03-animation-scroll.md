# Sprint-03 · Animation & Scroll

**Goal:** Add scrollytelling animations and smooth scrolling across the site.
**Spec ref:** [PROJECT_SPEC.md](../../PROJECT_SPEC.md) § Style rules (no flashy/distracting motion)
**Opened:** session 2
**Closed:** session 2

---

## Tasks

- [x] GSAP + ScrollTrigger installed and registered (`gsap`, `@gsap/react`)
- [x] Lenis smooth scroll initialized in `src/main.tsx`
- [x] `Reveal` component built (fade/slide in on scroll via ScrollTrigger)
- [x] `ScrollProgressBar` component built
- [x] `StaggerGroup` + `StaggerItem` components built
- [x] Animations applied across homepage sections and focus pages
- [x] `typecheck` + `build` passing

---

## Done When

Scroll animations visible in browser on all major sections; no animation library errors; build passes.

---

## Notes

Kept animations subtle per spec (calm, credible tone). No GSAP SplitText; character-level reveals were considered too flashy for this audience.
